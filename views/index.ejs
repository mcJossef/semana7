<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/index.css">
    <title>Clientes</title>
</head>

<body>
    <nav class="navbar navbar-expand navbar-light bg-faded">
        <div class="container">
            <a class="navbar-brand" href="/">Tarea semana 6</a>
            <ul class="navbar-nav me-auto mt-2 mt-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="/" aria-current="page">Clientes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/proveedor" aria-current="page">Proveedores</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/insumo" aria-current="page">Insumos</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container mt-5">
        <h2>Lista de Clientes</h2>
        <a href="/agregar" class="btn btn-primary mb-3">Agregar Cliente</a>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Apellidos</th>
                    <th>Dirección</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <% if (clientes.length> 0) { %>
                    <% clientes.forEach(function(cliente) { %>
                        <tr>
                            <td>
                                <%= cliente.nombre %>
                            </td>
                            <td>
                                <%= cliente.apellidos %>
                            </td>
                            <td>
                                <%= cliente.direccion %>
                            </td>
                            <td>
                                <a href="/editar/<%= cliente._id %>" class="btn btn-warning">Editar</a>
                                <form action="/eliminar/<%= cliente._id %>" method="POST" style="display:inline;">
                                    <button class="btn btn-danger">Eliminar</button>
                                </form>
                            </td>
                        </tr>
                        <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="4">No se encontraron clientes</td>
                                </tr>
                                <% } %>
            </tbody>
        </table>
    </div>
</body>

</html>