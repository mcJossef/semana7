<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/index.css">
    <title>Insumos</title>
</head>
<body>
    <nav class="navbar navbar-expand navbar-light bg-faded">
        <div class="container">
            <a class="navbar-brand" href="/">Tarea semana 6</a>
            <ul class="navbar-nav me-auto mt-2 mt-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="/">Clientes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/proveedor">Proveedores</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/insumo">Insumos</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container mt-5">
        <h2>Lista de Insumos</h2>
        <a href="/insumo/agregar" class="btn btn-primary mb-3">Agregar Insumo</a>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nombre del Insumo</th>
                    <th>Proveedor</th>
                    <th>Precio Unitario</th>
                    <th>Stock</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <% if (insumos.length > 0) { %>
                    <% insumos.forEach(function(insumo) { %>
                        <tr>
                            <td><%= insumo.nominsumo %></td>
                            <td><%= insumo.idprovedor ? insumo.idprovedor.nombrecia : 'N/A' %></td>
                            <td><%= insumo.preUni %></td>
                            <td><%= insumo.stock %></td>
                            <td>
                                <a href="/insumo/editar/<%= insumo._id %>" class="btn btn-warning">Editar</a>
                                <form action="/insumo/eliminar/<%= insumo._id %>" method="POST" style="display:inline;">
                                    <button class="btn btn-danger">Eliminar</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="5">No se encontraron insumos</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
