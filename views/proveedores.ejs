<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/index.css">
    <title>Proveedores</title>
</head>
<body>
    <nav class="navbar navbar-expand navbar-light bg-faded">
        <div class="container">
            <a class="navbar-brand" href="/">Tarea semana 6</a>
            <ul class="navbar-nav me-auto mt-2 mt-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="/">Clientes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/proveedor">Proveedores</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/insumo">Insumos</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container mt-5">
        <h2>Lista de Proveedores</h2>
        <a href="/proveedor/agregar" class="btn btn-primary mb-3">Agregar Proveedor</a>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nombre de la Compañía</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <% if (proveedores.length > 0) { %>
                    <% proveedores.forEach(function(proveedor) { %>
                        <tr>
                            <td><%= proveedor.nombrecia %></td>
                            <td>
                                <a href="/proveedor/editar/<%= proveedor._id %>" class="btn btn-warning">Editar</a>
                                <form action="/proveedor/eliminar/<%= proveedor._id %>" method="POST" style="display:inline;">
                                    <button class="btn btn-danger">Eliminar</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="2">No se encontraron proveedores</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
